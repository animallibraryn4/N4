version: '3.8'

services:
  bot:
    build: .
    container_name: telegram-link-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=your_bot_token_here
      - ADMINS=admin1_id,admin2_id
      - DB_URI=mongodb_uri_or_sql_connection_string
    volumes:
      - ./:/app
